<output xmlns="http://www.openrat.de/template" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xsi:schemaLocation="http://www.openrat.de/template ../../../../../../../template_engine/components/template.xsd">
	<group>
		<fieldset class="line" label="${message:name}">
			<text value="${name}" class="name"/>
		</fieldset>
		<fieldset class="line" label="${message:filename}">
			<text value="${filename}" class="filename"/>
		</fieldset>
		<fieldset class="line" label="${message:file_extension}">
			<text value="${extension}" class="extension"/>
		</fieldset>
		<fieldset class="line" label="${message:description}">
			<text value="${description}"/>
		</fieldset>
	</group>
	<group title="${message:additional_info}">
		<fieldset class="line" label="${message:full_filename}">
			<text value="${full_filename}"/>
		</fieldset>
		<fieldset class="line" label="${message:FILE_SIZE}">
			<text value="${size}"/>
		</fieldset>
		<fieldset class="line" label="${message:FILE_mimetype}">
			<text value="${mimetype}"/>
		</fieldset>
		<fieldset class="line" label="">
			<part class="value,act-clickable">
				<link class="action" type="dialog" subaction="size">
					<text value="${message:menu_file_size}"/>
				</link>
			</part>
		</fieldset>
		<fieldset class="line" label="${message:id}">
			<text value="${objectid}"/>
		</fieldset>

		<if present="cache_filename">
			<fieldset class="line" label="${message:CACHE_FILENAME}">
				<part>
					<text value="${cache_filename}"/>
					<newline/>
					<image icon="el_date"/>
					<date date="${cache_filemtime}"/>
				</part>
			</fieldset>
		</if>
		<fieldset class="line" label="${message:FILE_PAGES}">
			<table>
				<list list="${pages}" extract="true">
					<row>
						<column>
							<link url="${url}" target="cms_main">
								<image type="page"/>
								<text value="${name}"/>
							</link>
						</column>
					</row>
				</list>
			</table>
			<if empty="${pages}">
				<text value="${message:NOT_FOUND}"/>
			</if>
		</fieldset>
	</group>
	<group title="${message:validity}">
		<part class="act-clickable">
			<link subaction="settings" type="dialog">
				<fieldset class="line" label="${message:state}">
					<if true="${is_valid}">
						<text value="${message:is_yes}"/>
					</if>
					<else>
						<text value="${message:is_no}"/>
					</else>
				</fieldset>
				<fieldset class="line" label="${message:from}">
					<date date="${valid_from_date}"/>
				</fieldset>
				<fieldset class="line" label="${message:until}">
					<date date="${valid_to_date}"/>
				</fieldset>
			</link>
		</part>
	</group>
	<group title="${message:prop_userinfo}">
		<fieldset class="line" label="${message:created}">
			<image elementtype="date"/>
			<date date="${create_date}"/>
			<newline/>
			<image action="user"/>
			<user user="${create_user}"/>
		</fieldset>
		<fieldset class="line" label="${message:lastchange}">
			<image elementtype="date"/>
			<date date="${lastchange_date}"/>
			<image action="user"/>
			<user user="${lastchange_user}"/>
		</fieldset>
		<fieldset class="line" label="${message:published}">
			<image elementtype="date"/>
			<date date="${published_date}"/>
			<newline/>
			<image action="user"/>
			<user user="${published_user}"/>
		</fieldset>
	</group>

	<group title="${message:settings}">

		<table filter="true">
			<list list="${settings}" value="entry">
				<row class="data">
					<column>
						<text value="${entry.key}"/>
					</column>
					<column class="act-clickable">
						<link subaction="settings" type="dialog">
							<text value="${entry.value}"/>
						</link>
					</column>
				</row>
			</list>
		</table>
	</group>
</output>
