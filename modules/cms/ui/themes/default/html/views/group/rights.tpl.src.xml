<output xmlns="http://www.openrat.de/template" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.openrat.de/template ../../../../../../../template_engine/components/template.xsd">
    <table filter="false">
        <list list="${projects}" extract="true">
            <row>
                <column>
                    <group title="${projectname}">
                        <if empty="${rights}">
                            <row>
                                <column>
                                    <text value="${message:GLOBAL_NOT_FOUND}"></text>
                                </column>
                            </row>
                        </if>
                        <if not="true" empty="${rights}">
                            <table>
                                <row class="headline">
                                    <column class="help">
                                        <text value="${message:GLOBAL_USER}"></text>
                                    </column>
                                    <column class="help">
                                        <text value="${message:GLOBAL_NAME}"></text>
                                    </column>
                                    <column class="help">
                                        <text value="${message:GLOBAL_LANGUAGE}"></text>
                                    </column>
                                    <list list="${show}" value="t">
                                        <column class="help">
                                            <text value="${message:${t}}" prefix="acl_" suffix="_abbrev"
                                                  title="message:acl_{t}"></text>
                                        </column>
                                    </list>
                                </row>
                                <list list="${rights}" key="aclid" value="acl" extract="true">
                                    <row class="data clickable">
                                        <column>
                                            <if present="groupname">
                                                <image action="group"></image>
                                                <text value="${groupname}" maxlength="20"></text>
                                            </if>
                                            <if not="true" present="username">
                                                <if not="true" present="groupname">
                                                    <image action="group"></image>
                                                    <text value="${message:global_all}"></text>
                                                </if>
                                            </if>
                                            <set var="username"></set>
                                            <set var="groupname"></set>
                                        </column>
                                        <column title="${objectname}">
                                            <image action="${objecttype}"></image>
                                            <link type="open" action="${objecttype}" subaction="" id="${objectid}">
                                                <text value="${objectname}"></text>
                                            </link>
                                        </column>
                                        <column>
                                            <text value="${languagename}" maxlength="20"></text>
                                        </column>
                                        <list list="${show}">
                                            <column>
                                                <set var="${list_value}" value="${bits}" key="${list_value}"></set>
                                                <checkbox name="${list_value}" readonly="true"></checkbox>
                                            </column>
                                        </list>
                                    </row>
                                </list>
                            </table>
                        </if>
                    </group>
                </column>
            </row>
        </list>
    </table>
</output>