<output xmlns="http://www.openrat.de/template" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.openrat.de/template ../../../../../../../template_engine/components/template.xsd">
  <table>
    <row class="headline">
      <column>
        <image type="user"/>
        <text value="${message:name}"/>
      </column>
      <column>
        <text value=""/>
      </column>
      <column>
        <text value="${message:LOGIN}"/>
      </column>
    </row>
    <list list="${el}" extract="true">
      <row class="data">
        <column id="${id}" name="${name}" action="user" class="clickable">
          <link type="open" id="${id}" name="${name}" action="user">
            <image action="user"/>
            <text value="${name}"/>
          </link>
        </column>
        <column id="${id}" name="${name}" action="user">
          <text value="${fullname}"/>
          <if true="${isAdmin}">
            <text value="_("/>
            <text value="${message:USER_ADMIN}"/>
            <text value=")"/>
          </if>
        </column>
        <column class="clickable">
          <link type="post" action="user" subaction="switch" id="${userid}">
            <text value="${message:LOGIN}"/>
          </link>
        </column>
      </row>
    </list>
    <row class="data">
      <column colspan="3" class="clickable">
        <link type="dialog" subaction="add" name="${message:add}">
          <image method="add"/>
          <text value="${message:new}"/>
        </link>
      </column>
    </row>
  </table>
</output>
