<output xmlns="http://www.openrat.de/template" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.openrat.de/template ../../../../../../../template_engine/components/template.xsd">
  <form action="login" subaction="login" target="_top">
    <!-- Open-Id -->
    <if true="${config:security/openid/enable}">
      <group title="${message:OPENID}">
        <fieldset class="line" label="">
          <part class="label">
            <text value="${message:openid_user}"/>
            <if not="true" empty="${config:security/openid/logo_url}">
              <image url="${config:security/openid/logo_url}"/>
            </if>
          </part>
          <part class="value">
            <radiobox name="openid_provider" list="openid_providers"/>
            <if true="${openid_user_identity}">
              <radio name="openid_provider" value="identity"/>
              <input name="openid_url" class="name" size="20"/>
            </if>
          </part>
        </fieldset>
      </group>
      <if value="size:dbids" greaterthan="1">
        <group title="${message:DATABASE}" icon="database">
          <fieldset class="line" label="">
            <part class="label">
              <label for="dbid">
                <text value="${message:DATABASE}"/>
              </label>
            </part>
            <part class="value">
              <selectbox name="dbid" list="dbids" default="${actdbid}"/>
              <hidden name="screenwidth" default="9999"/>
            </part>
          </fieldset>
        </group>
      </if>
      <else>
        <hidden name="dbid" default="${actdbid}"/>
      </else>
      <hidden name="objectid"/>
      <hidden name="modelid"/>
      <hidden name="projectid"/>
      <hidden name="languageid"/>
    </if>
    <else>
      <part class="message error">
        <text value="${message:OPENID_NOT_ENABLED}"/>
      </part>
    </else>
  </form>
  <!-- <newline></newline> <newline></newline> <link url="${config:login/gpl/url}"
		target="_top" class="copyright"> <text value="${message:GPL}"></text> </link> -->
</output>
