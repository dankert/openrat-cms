<output xmlns="http://www.openrat.de/template" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.openrat.de/template ../../../../../../../template_engine/components/template.xsd">
  <table>
    <row header="true">
      <column>
        <text value="${message:name}"/>
      </column>
    </row>
    <list list="${el}" extract="true">
      <row class="data">
        <column id="${id}" name="${name}" action="group" class="clickable">
          <link type="open" id="${id}" name="${name}" action="group">
            <image action="group"/>
            <text value="${name}" maxlength="30"/>
          </link>
        </column>
      </row>
    </list>
    <row class="data">
      <column colspan="2" class="clickable">
        <link type="dialog" subaction="add" name="${message:menu_add}">
          <image method="add"/>
          <text value="${message:new}"/>
        </link>
      </column>
    </row>
  </table>
</output>
