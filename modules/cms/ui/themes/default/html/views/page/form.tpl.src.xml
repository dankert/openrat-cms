<output xmlns="http://www.openrat.de/template" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xsi:schemaLocation="http://www.openrat.de/template ../../../../../../../template_engine/components/template.xsd">
	<form>
		<table>
			<if empty="${el}">
				<row>
					<column colspan="4">
						<text value="${message:NOT_FOUND}"/>
					</column>
				</row>
			</if>
			<if not="" empty="${el}">
				<row>
					<column class="help">
						<text value="${message:PAGE_ELEMENT_NAME}"/>
					</column>
					<column class="help">
						<text value="${message:CHANGE}"/>
					</column>
					<column class="help">
						<text value="${message:VALUE}"/>
					</column>
				</row>
				<list list="${el}" extract="true">
					<row class="data">
						<column>
							<label for="${saveid}">
								<image elementtype="${type}"/>
								<text value="${name}"/>
							</label>
						</column>
						<column>
							<checkbox name="${saveid}" default="false" readonly="false"/>
						</column>
						<column>
							<if value="${type}" contains="text,date,number">
								<input index="true" type="text" name="${id}" default="${value}" size="40"
									   maxlength="255" onchange="onchange"/>
							</if>
							<if value="${type}" equals="longtext">
								<inputarea index="true" name="${id}" rows="7" cols="50" onchange="onchange"
										   default="${value}"/>
							</if>
							<if value="${type}" contains="select,link,list">
								<selectbox name="${id}" list="list" default="${value}"/>
							</if>
						</column>
					</row>
				</list>
			</if>
		</table>
		<group title="${message:options}">
			<if present="release">
				<part>
					<checkbox name="release"/>
					<label for="release">
						<text value=" "/>
						<text value="${message:RELEASE}"/>
					</label>
				</part>
			</if>
			<if present="publish">
				<part>
					<checkbox name="publish"/>
					<label for="publish">
						<text value=" "/>
						<text value="${message:PAGE_PUBLISH_AFTER_SAVE}"/>
					</label>
				</part>
			</if>
		</group>
		<button type="ok"/>
	</form>
</output>
